services:
  - type: web
    name: fastapi-service
    env: docker
    dockerfilePath: Dockerfile
    plan: free
    healthCheckPath: "/health"
    autoDeploy: true

  - type: web
    name: streamlit-service
    env: docker
    dockerfilePath: Dockerfile
    plan: free
    autoDeploy: true




